<!DOCTYPE html>
<html lang="en">
  <head>
      <% include ../partials/head %>
  </head>
  <body>
    <% include ../partials/nav %>
    <div class="container">
      <div class="jumbotron">
        <h1 class="display-4">AIFEX </h1>
        <p class="lead">Improve the quality, efficiency and diversity of your exploratory test sessions</p>
        <hr class="my-4"></hr>
        <h2>SignIn / SignUp</h2>
        <% if (kind) { %>
          <div class="alert <%= kind %> " role="alert">
              <div>anonymous / anonymous</div>
              <%= message %>
          </div>
        <% } %>
        <form action="/account/sign" method="post" id="signin">
          <div class="form-group row">
            <label for="user" class="col-sm-2 col-form-label">Username</label>
            <div class="col-sm-10">
              <input type="text" class="form-control" form="signin" name="username" id="user" value="anonymous" placeholder="anonymous" required>
            </div>
          </div>

          <div class="form-group row">
            <label for="password" class="col-sm-2 col-form-label">Password</label>
            <div class="col-sm-10">
              <input type="password" class="form-control" name="password" id="password" value="anonymous" placeholder="anonymous"  required>
            </div>
          </div>
          
          <button type="submit" class="btn btn-primary">sign</button>
        </form>
      </div>
    </div>
  </body> 
</html>
